export interface CarType {
  id: number;
  name: string;
  color: string;
}

export interface EngineResponse {
  velocity: number;
  distance: number;
}

export interface WinnerType {
  id: number;
  wins: number;  
  time: number;   
}
